# Supabase環境変数テンプレート

# =======================
# Supabase 設定
# =======================

# Supabaseプロジェクトの基本URL
# 取得場所: Supabase Dashboard → Settings → API
SUPABASE_URL=https://your-project.supabase.co

# 匿名公開キー（クライアント側で使用）
# 取得場所: Supabase Dashboard → Settings → API → anon public key
SUPABASE_ANON_KEY=your-anon-public-key-here

# サービスロールキー（サーバー側でのみ使用）⚠️ シークレット
# 取得場所: Supabase Dashboard → Settings → API → service_role key (secret)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# =======================
# ストレージ設定
# =======================

# レシート画像保存用バケット名
SUPABASE_STORAGE_BUCKET=receipts

# =======================
# API設定（オプション）
# =======================

# API URL（通常は SUPABASE_URL と同じ）
SUPABASE_API_URL=${SUPABASE_URL}

# =======================
# 環境指定
# =======================

# development / staging / production
NODE_ENV=development

# =======================
# デバッグ設定（開発環境のみ）
# =======================

# Supabase ログレベル
SUPABASE_DEBUG=false

# =======================
# 使用方法
# =======================
#
# 1. Supabaseダッシュボードから以下の値を取得:
#    - SUPABASE_URL: Project Settings → API → Project URL
#    - SUPABASE_ANON_KEY: Project Settings → API → anon public key
#    - SUPABASE_SERVICE_ROLE_KEY: Project Settings → API → service_role key
#
# 2. 上記の "your-*-here" を実際の値で置き換える
#
# 3. このファイルを .env にリネーム
#
# 4. .env ファイルを .gitignore に追加（本番キーを保護するため）
#
# =======================
# セキュリティに関する注意
# =======================
#
# ⚠️ 重要：このファイルをGitにコミットしないこと！
#
# .gitignore に以下を追加してください:
#   .env
#   .env.local
#   .env.*.local
#
# 本番環境では:
#   - GitHub Secrets や環境変数で管理
#   - SERVICE_ROLE_KEY は絶対にクライアント側に公開しない
#   - 定期的にキーをローテーション
#
# =======================
